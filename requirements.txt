# ── Core ───────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# ── LangGraph & LangChain ─────────────────────────────────────
langgraph>=0.0.40
langchain-core>=0.1.20

# ── LLM ───────────────────────────────────────────────────────
groq>=0.4.0

# ── PDF Processing ────────────────────────────────────────────
pymupdf>=1.24.0

# ── Token Counting (PageIndex tree generation) ────────────────
tiktoken>=0.7.0

# ── Observability ─────────────────────────────────────────────
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
# opentelemetry-exporter-otlp — install separately if OTLP tracing is needed
prometheus-client>=0.19.0
structlog>=24.1.0
aiosqlite>=0.20.0

# ── Authentication ────────────────────────────────────────────
PyJWT[crypto]>=2.8.0

# ── Testing (dev only — not installed in production) ──────────
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.1.0
# httpx>=0.26.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.8.0
