# ═══════════════════════════════════════════════════════════════
# PageIndex Finance RAG — Environment Variables
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ─── Required ──────────────────────────────────────────────────
GROQ_API_KEY="Enter your groq api key"

# ─── Application ───────────────────────────────────────────────
ENV=development
LOG_LEVEL=INFO
DEBUG=false

# ─── Clerk Authentication (Backend JWT verification) ──────────
# Get from Clerk Dashboard → JWT Templates
# CLERK_ISSUER_URL=https://your-clerk-instance.clerk.accounts.dev
# CLERK_JWKS_URL=https://your-clerk-instance.clerk.accounts.dev/.well-known/jwks.json

# ─── Convex (optional for backend — used by frontend) ─────────
# CONVEX_URL=https://your-project.convex.cloud

# ─── CORS (production only) ───────────────────────────────────
# Comma-separated origins. Defaults to http://localhost:3000.
# ALLOWED_ORIGINS=https://finsight.vercel.app,http://localhost:3000

# ─── PageIndex Settings (optional — defaults shown) ───────────
# TREE_GEN_MODEL=openai/gpt-oss-120b
# TREE_SEARCH_MODEL=openai/gpt-oss-120b
# PDFS_DIR=data/pdfs
# TREES_DIR=data/trees
# METADATA_DB_PATH=data/pageindex_metadata.db
# MAX_TREE_DEPTH=5
# MAX_PAGES_PER_NODE=10
# MAX_PDF_SIZE_MB=100
# MAX_PDF_PAGES=1000
# MAX_QUERY_LENGTH=2000
# MAX_SEARCH_RETRIES=2
# TREE_SEARCH_BREADTH=3

# ─── API ───────────────────────────────────────────────────────
# API_HOST=0.0.0.0
# API_PORT=8000

# ─── Observability (optional) ─────────────────────────────────
# OTLP_ENDPOINT=http://localhost:4317
# PROMETHEUS_PORT=9090
